(define (fizzBuzz num)
  (if ((= num 0))
    '0
    (
      (if ()
      )
    )
  )
)

(define (divBy5 num)
  (if ()
    ()
  )
)

;;; this is WIP on a fizzbuzz in scheme, want it to use recursion and no
;;; fancy data structures or types, strings and concatenation all the way
