; no this procedure will not work
; this is because when the second let is evaluated define dy
; will will use y at a time when using y will throw an error
; the normal version would fail too if it wasn't for the delay
; in the define y
