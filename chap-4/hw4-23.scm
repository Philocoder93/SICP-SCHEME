; in the case of one expression
; the first procedure just is the correct procedure call
; the second one has to do a check on two memory
; locations and progress through a cond block before
; calling it
;
; in the case of two expressions
; the first procedure calls both sub procedures in it's body
; the second one has go through two cond blocks access
; five memory locations and do a recursive call

