; the literal meaning of ' is something like
; "wrap the very next expression in a quote call"
; so when you type ''abracadabra the first 'wraps the
; next expression *the other '* in a quote call
; it's equivalent to typing (quote quote)
